package test;

import java.util.ArrayList;
import java.util.Set;

import dao.DataKit;
import dao.DbRead;
import entity.Student;
import entity.Teacher;
import util.DataInitialization;

public class Test {

	public static void main(String[] args) {
//		DataInitialization.initteacher();
//		DataInitialization.initstudent();
		ArrayList<Student> arr =null;
		ArrayList<Teacher> tea = null;
		Set<Student> stuset;
		arr = DbRead.readstuinfo();
		for (int i = 0; i < arr.size(); i++) {
			System.out.println(arr.get(i).getID() + "-" + arr.get(i).getName() + "-" + arr.get(i).getScore().getTotalscores());
		}
		tea = DbRead.readteacherinfo();
//		for (int i = 0; i < tea.size(); i++) {
//			System.out.println(tea.get(i).getName() + "-" +tea.get(i).getCourse() );
//		}
//		
		stuset = DataKit.selectstu(arr);
		for(Student stu : stuset) {
			System.out.println(stu.getName() + "-" + stu.getScore().getTotalscores());
		}
		
		
		
		
		
	}

}
